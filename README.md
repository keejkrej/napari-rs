# napari-rs

Rust port of selected core napari functionality.

This repository is being built incrementally from the sibling Python source tree
at `../napari`. The first ported slice is `napari.utils.geometry`.
